# Control flow graphs, possibly rest of the NOLTIS algorithm.

After some linear programs can be successfully compiled, it would make not sense to not get on compiling programs with structured control flow.
